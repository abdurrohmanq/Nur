namespace Nur.APIService.Constants;

public class HttpConstant
{
    private const string scheme = "http";
    private const string domain = "localhost";

    public const string BaseLink = $"{scheme}://{domain}/";
}
